import React from 'react';
import FormContainer from "../components/FormContainer";

const CartScreen = () => {
    return (
        <FormContainer>
            <h1>Cart</h1>

        </FormContainer>
    );
};

export default CartScreen;
